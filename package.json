{
  "name": "your-flask-app",
  "version": "1.0.0",
  "scripts": {
    "start": "python main.py",
    "vercel-build": "python3.8 -m pip install -r requirements.txt"
  },
  "dependencies": {
    "flask": "^2.0.1"
    // Add any other dependencies here if needed
  }
}
